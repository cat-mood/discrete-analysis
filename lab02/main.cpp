#include <iostream>
#include <string>
#include "patricia.h"

struct str {
    int data;
    str* next;
};

int main() {
    // str* s = new str();
    // s->data = 1;
    // s->next = s;
    // std::cout << s->data << std::endl;
    // std::cout << s->next->data << std::endl;
    // str* p = new str();
    // p->data = 2;
    // str*& q = s->next;
    // q = p;
    // delete q;
    // str* t = new str();
    // t->data = 3;
    // q = t;
    // std::cout << s->data << std::endl;
    // std::cout << s->next->data << std::endl;
    // TPatriciaTrie t;
    // t.Insert({"aaa", 40});

    return 0;
}
